name: credit-risk-classification

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir:
        type: str
        default: credit_risk_preprocessing
    command: >
      python modelling.py --data_dir {data_dir}
